<div class="row news-row">
    {{#if data}}
        {{#each data}}
            {{!-- <div class="grid-sizer col s12 m6">
                <div id="card-block" class="card center news grey lighten-5">
                    <div class="card-stacked">
                        <div class="card-content">
                            <span class="card-title">{{this.title}}</span>
                            <p class="hide-on-small-only grey-text text-darken-4">{{this.description}}</p>
                        </div>
                        <div class="row">
                            <div class="col s12">
                                <form action="/save/{{this._id}}?_method=PUT" id="save" method="POST">
                                    <button type="submit" class="grey darken-1 btn waves-light">Save</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div> --}}
            <div class="news col s12 m6">
                <div class="card horizontal left-align">
                    <div class="card-stacked">          
                    <div class="card-content">              
                        <p class="flow-text">{{this.title}}</p>
                        <a href="{{this.link}}">
                            <p class="hide-on-small-only">{{this.description}}</p> 
                        </a>             
                    </div>
                    <div class="card-action">
                        <form action="/save/{{this._id}}?_method=PUT" id="save" method="POST">
                            <button type="submit" class="grey darken-1 btn waves-light">Save</button>
                        </form>
                    </div>
                    </div>
                </div>
            </div>
        {{/each}}
        {{else}}
        <h2 class="grey-text text-darken-4">There's nothing to show yet...
            Do some scraping and try again!
        </h2>
    {{/if}}
</div>

        {{!-- <div class="news col s12 m6">
          <div class="card horizontal left-align">>
            <div class="card-stacked">          
              <div class="card-content">              
                <p class="flow-text">{{this.title}}</p>
                <p class="hide-on-small-only">{{this.description}}</p>              
              </div>
              <div class="card-action">
                <form action="/save/{{this._id}}?_method=PUT" id="save" method="POST">
                    <button type="submit" class="grey darken-1 btn waves-light">Save</button>
                </form>
              </div>
            </div>
          </div>
        </div> --}}
